File Name -- Course.java
1. enrollStudent() - Adding a check for checking capacity of the course.
2. dropStudent() - Dropping student by checking if enrolled count is not < 0
3. getCourseLocation() - Returning courseLocation instead of instructorName
4. getInstructorName() - Returning instructorName instead of courseLocation
5. isCourseFull() - Returning the opposite
 
PMD Identified 
1. MissingOverride - The method 'toString()' is missing an @Override annotation.


File Name -- Department.java
1. getNumberOfMajors() - Negative value is returned 
2. getDepartmentChair() - String of the variable is returned 
3. dropPersonFromMajor() - Checking if majors are > 0 , before reducing  

PMD Identified
1. LooseCoupling - 
2. Override toString() - Missing override
3. AvoidLiteralsInIfCondition - Declaring the Minimum Major Count and then using it in function dropPersonFromMajor()


File Name -- MyFileDatabase.java

PMD Identified 
1. MyFileDatabase.deSerializeObjectFromFile() - Overridable method called during object construction
2.  System.out/err - Using LOGGER instead of System.out.println in  saveContentsToFile
3. Returning empty collection rather than null in deSerializeObjectFromFile() 
4. LooseCoupling - Avoid using HashMap


File Name -- RouteController.java
1. findCourseTime() - Changing the return, basically changing time slot.
2. retrieveCourse() - Changing return status
3. retrieveDepartment() - swapping the return status
4. getMajorCtFromDept() - Negative value returning
5. handleException() - Wrong return status it should be INTERNAL_SERVER_ERROR
6. Changing courseCode to COURSE_CODE_PARAM
7. Changing deptCode to DEPT_CODE_PARAM
8. "Course not found" literal occuring many times
9. "Department Not found" literal occuring many times 
10. retrieveDepartment() calling upper case function more than once


PMD Identified
1. AvoidDuplicateLiterals
2. Unused local variale 

